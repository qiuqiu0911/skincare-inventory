.review-page {
  min-height: 100vh;
}

.review-tabs {
  margin-bottom: var(--space-7);
}

.tab-icon {
  width: 32rpx;
  height: 32rpx;
  margin-right: var(--space-2);
  flex-shrink: 0;
}

/* ========== 三日列表 ========== */
.recent-list {
  display: flex;
  flex-direction: column;
  gap: var(--space-5);
}

.day-card {
  padding: var(--space-6);
  border-radius: var(--radius-lg);
}

.day-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-4);
  margin-bottom: var(--space-5);
  padding-bottom: var(--space-4);
  border-bottom: 1rpx solid var(--color-border-subtle);
}

.day-title {
  color: var(--color-text-primary);
  font-size: var(--fs-subtitle);
  font-weight: var(--fw-bold);
  line-height: var(--lh-subtitle);
}

.day-date {
  margin-top: var(--space-1);
  color: var(--color-text-tertiary);
  font-size: var(--fs-body-sm);
  line-height: var(--lh-body-sm);
}

.makeup-button {
  min-width: 120rpx;
  min-height: var(--tap-min);
  padding: 0 var(--space-5);
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-pill);
  background: var(--color-surface-muted);
  color: var(--color-text-primary);
  font-size: var(--fs-body-sm);
  font-weight: var(--fw-semibold);
  flex-shrink: 0;
  transition: opacity var(--motion-fast) var(--ease-standard);
}

.slot-block {
  display: flex;
  align-items: flex-start;
  gap: var(--space-4);
  margin-top: var(--space-5);
}

.slot-icon {
  width: 56rpx;
  height: 56rpx;
  flex-shrink: 0;
  padding: var(--space-3);
  border-radius: var(--radius-pill);
  box-sizing: border-box;
}

.sun {
  background: var(--color-routine-morning-soft);
}

.moon {
  background: var(--color-routine-evening-soft);
}

.slot-list {
  flex: 1;
  min-width: 0;
}

.slot-row {
  color: var(--color-text-primary);
  font-size: var(--fs-body);
  line-height: var(--lh-body);
  word-break: break-all;
}

.slot-amount {
  margin-left: var(--space-3);
  color: var(--color-text-secondary);
  font-size: var(--fs-caption);
  line-height: var(--lh-caption);
}

.empty-line {
  color: var(--color-text-secondary);
  font-size: var(--fs-body-sm);
  line-height: var(--lh-body-sm);
}

/* ========== 周视图 ========== */
.week-card {
  padding: var(--space-6);
  border-radius: var(--radius-lg);
}

.week-panel {
  min-height: 320rpx;
}

.week-empty {
  margin: 0;
  border-radius: var(--radius-lg);
}

.week-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-4);
}

.week-title {
  color: var(--color-text-primary);
  font-size: var(--fs-body);
  font-weight: var(--fw-semibold);
  line-height: var(--lh-body);
}

.week-range {
  color: var(--color-text-secondary);
  font-size: var(--fs-caption);
  line-height: var(--lh-caption);
}

.week-legend {
  margin-bottom: var(--space-5);
  display: flex;
  align-items: center;
  gap: var(--space-5);
  color: var(--color-text-secondary);
  font-size: var(--fs-caption);
  font-weight: var(--fw-semibold);
  line-height: var(--lh-caption);
}

.legend-item {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.legend-half {
  width: 24rpx;
  height: 24rpx;
  border-radius: var(--radius-pill);
}

.legend-morning {
  border-radius: var(--radius-pill) 0 0 var(--radius-pill);
  background: var(--color-routine-morning);
}

.legend-evening {
  border-radius: 0 var(--radius-pill) var(--radius-pill) 0;
  background: var(--color-routine-evening);
}

.week-grid-head,
.week-row {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.week-grid-head {
  margin-bottom: var(--space-4);
  padding-bottom: var(--space-3);
  border-bottom: 1rpx solid var(--color-border-subtle);
  color: var(--color-text-secondary);
  font-size: var(--fs-caption);
  font-weight: var(--fw-semibold);
}

.week-row {
  min-height: 64rpx;
  border-bottom: 1rpx solid var(--color-border-subtle);
}

.week-row:last-of-type {
  border-bottom: 0;
}

.product-col {
  width: 200rpx;
  flex-shrink: 0;
}

.product-name {
  overflow: hidden;
  color: var(--color-text-primary);
  font-size: var(--fs-body-sm);
  line-height: var(--lh-body-sm);
  text-overflow: ellipsis;
  white-space: nowrap;
}

.day-cols {
  flex: 1;
  display: flex;
  justify-content: space-between;
  min-width: 0;
}

.day-col {
  width: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.today-col {
  color: var(--color-text-primary);
  font-weight: var(--fw-bold);
}

.usage-circle {
  width: 28rpx;
  height: 28rpx;
  display: flex;
  overflow: hidden;
  border-radius: var(--radius-pill);
  box-shadow: 0 0 0 2rpx var(--color-border-subtle);
}

.usage-half {
  width: 50%;
  height: 100%;
  background: var(--color-surface-muted);
}

.usage-half-morning-on {
  background: var(--color-routine-morning);
}

.usage-half-evening-on {
  background: var(--color-routine-evening);
}

/* ========== 周总结 ========== */
.week-summary {
  margin-top: var(--space-6);
  padding: var(--space-5);
  border-radius: var(--radius-lg);
  background: var(--color-surface-soft);
}

.summary-kicker {
  color: var(--color-text-secondary);
  font-size: var(--fs-caption);
  font-weight: var(--fw-semibold);
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.summary-grid {
  display: flex;
  gap: var(--space-4);
  margin-top: var(--space-4);
}

.summary-box {
  flex: 1;
  min-width: 0;
  padding: var(--space-5);
  border-radius: var(--radius-md);
  background: var(--color-surface-elevated);
  text-align: center;
}

.summary-value {
  color: var(--color-text-primary);
  font-size: var(--fs-title);
  font-weight: var(--fw-bold);
  line-height: var(--lh-title);
}

.summary-label {
  margin-top: var(--space-1);
  color: var(--color-text-secondary);
  font-size: var(--fs-caption);
  line-height: var(--lh-caption);
}
